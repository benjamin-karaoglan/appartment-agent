# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# App URL (for Better Auth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (for Better Auth server-side)
DATABASE_URL=postgresql://appart:appart@localhost:5432/appart_agent

# Better Auth
# Generate a random 32+ character string for BETTER_AUTH_SECRET
BETTER_AUTH_SECRET=your-better-auth-secret-at-least-32-characters
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth (OPTIONAL — email/password login works without these)
# Leave empty to disable Google sign-in. See .env.example in project root for setup steps.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
